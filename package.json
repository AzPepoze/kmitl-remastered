{
	"name": "kmitl-remastered",
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json",
		"release": "pnpm run build && mkdir -p release && cd dist && zip -r ../release/chrome.zip . && zip -r ../release/firefox.zip . && cd .."
	},
	"devDependencies": {
		"@sveltejs/vite-plugin-svelte": "^5.1.0",
		"@tsconfig/svelte": "^5.0.4",
		"svelte": "^5.34.7",
		"svelte-check": "^4.2.2",
		"typescript": "~5.8.3",
		"vite": "^7.0.0"
	}
}
